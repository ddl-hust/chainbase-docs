---
title: 'Run a Operator(Not released to mainnet yet)'
---

## Requirements

➕[Docker](https://docs.docker.com/get-docker/): Docker is a requirement for AVS operators.

➕[Docker Compose](https://docs.docker.com/compose/install/):  Docker Compose is used alongside Docker for executing Operator software. 

➕Linux Environment: Eigenlayer’s CLI only runs in a Linux environment.

Run the following commands to clone the Oprator Setup Repo and create a new environment file from the provided template. 

```
git clone https://github.com/Layr-Labs/chainbase-avs-setup.git
cd chainbase-avs-setup
cd holesky && cp .env.example .env
```

The provided `.env` contains many default configuration settings for the node. All sections marked with TODO must be updated to match your environment. 

**Configure local storage locations**

Check that the `$USER_HOME`, `$EIGENLAYER_HOME`, `$CHAINBASE_AVS_HOME` are properly set within your environment file and that all the folders exist as expected.


## EigenLayer Registration

Before registering as an AVS, ensure that the operator has already registered with EigenLayer. 
For reference, see [here](https://docs.eigenlayer.xyz/eigenlayer/operator-guides/operator-installation)

## Chainbase AVS Registration

If you have successfully registered as an EigenLayer operator, you can execute the following command from the `chainbase-operator-setup` folder to register AVS:

```shell
./chainbase-avs.sh register
```



## Run

Execute the following command to start the docker containers:

```shell
./chainbase-avs.sh run
```

## View logs

You may view the container logs using and of the following commands

```shell
 docker-compose logs -f  chainbase-node
```

Upon successfully starting up, the Chainbase node should produce logs similar to the following:

```shell
chainbase-node  | time=2024-08-05T09:23:21.650Z level=INFO msg=operator address=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52
chainbase-node  | time=2024-08-05T09:23:21.651Z level=INFO msg="AVS is registered,continue"
chainbase-node  | time=2024-08-05T09:23:22.730Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:23:39.008Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:23:54.781Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:24:10.597Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:24:26.396Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:24:42.250Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
chainbase-node  | time=2024-08-05T09:24:58.009Z level=INFO msg="update host metrics" avsAddr=0x20E67b6FbF0C1eCbCA2f0f6A8ffD0f0DA0031B52 ip=188.253.117.10 job_manager_status=1
```